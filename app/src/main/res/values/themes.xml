<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.DTT" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- Primary brand color. -->
        <!-- Primary brand color -->
        <item name="colorPrimary">@color/md_primary</item>
        <item name="colorPrimaryVariant">@color/md_primary_variant</item>
        <item name="colorSecondary">@color/md_secondary</item>

        <!-- Surface and background colors -->
        <item name="colorSurface">@color/md_surface</item>
        <item name="colorOnSurface">@color/md_on_surface</item>

        <!-- Text and icon colors -->
        <item name="android:textColorPrimary">@color/md_on_surface</item>
        <item name="android:textColorSecondary">@color/md_surface</item>

        <!-- Others -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimary</item>

    </style>

    <style name="Theme.DailyTaskTracker" parent="Base.Theme.DailyTaskTracker" />
</resources>